<%
toEth = function(wei) {
  return wei / Math.pow(10, 18)
}

isPending = function(pending, recent) {
  if (!pending || pending < recent) {
    return false
  }
  
  currentTime = Math.ceil(new Date().getTime() / 1000)
  waiting = 45
  if (pending + waiting <= currentTime) {
    return false
  }

  return true
}
%>